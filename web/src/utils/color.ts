const colors = { blue: '#2196F3' };
const borderColors = { blue: '#2196F3' };

export const chartColors = Object.values(colors);
export const chartBorderColors = Object.values(borderColors);

export default colors;
